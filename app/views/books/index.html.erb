<h1><b>Books</b></h1>

  <table>
    <tr><th>Title</th>
    <th>Body</th></tr>

<% @books.each do |book| %>


    <tr><td><%= book.title %></td>
    <td><%= book.body %></td>
    <td><%=link_to "Show", book_path(book),class:"show_#{book.id}"%></td>
    <td><%=link_to "Edit", edit_book_path(book) %></td>
　  <td><%=link_to "Destroy", book_path(book), method: :delete,class:"destroy_#{book.id}" %></td></tr>
     <% end %>
  </table>



<h2>New books</h2>

<%= form_for(@newbook) do |f| %>

    <p>Title</p>
    <%= f.text_field :title,class:"book_title" %>

    <p>Body</p>
    <%= f.text_area :body,class:"book_body" %>

    <p><%= f.submit 'Create Book' %></p>


<% end %>